# Phi Brain — 공통 작업 지침

- 작업 전에 `docs/PRODUCT.md`, `docs/DESIGN.md`, `docs/STATUS.md`를 읽는다. 다른 도구에서 작업을 이어받을 때도 다시 읽는다.
- 제품·디자인의 공통 기준은 위 문서에서 관리한다. 이 파일과 `CLAUDE.md`에 기획 내용을 중복 작성하지 않는다.
- 사용자 확정 사항, 에이전트 제안, 미결정 사항을 구분한다. 단순한 제안을 사용자 승인으로 기록하지 않는다.
- 현재는 Journaling·Future Items 프런트엔드 프로토타입 단계다. 승인된 화면 구현 이력을 되돌리지 않는다. 새로운 기능 구현은 사용자 요청 범위에 맞춰 진행한다. Assignment Manage는 요구사항만 정리되었고 아직 미구현이다. 서버·DB·Gmail 연동도 현재 구현되어 있지 않다.
- 작업 시작 전에 Git 상태와 기존 변경을 확인한다. 다른 도구나 사용자의 변경을 덮어쓰거나 되돌리지 않는다.
- 작업 후 `docs/STATUS.md`에 담당 도구, 변경 파일, 검증 결과, 남은 일과 인계 사항을 갱신한다. 진행 중 작업은 작업 시작 시 표시한다.
- 제품·디자인 결정이 바뀌면 해당 공통 문서도 갱신한다. 충돌하는 결정은 임의로 확정하지 않고 미결정으로 남긴다.
- 같은 파일의 동시 편집을 피한다. 상태 문서는 협업 안내이며 자동 잠금 기능이 아니다.
- 실제 저널, 개인정보, API 키는 Git에 넣지 않는다. 예시는 가상 데이터임을 표시한다.
- OneDrive 이전과 동기화 구성은 아직 진행하지 않는다. 사용자와 경로·작업 방식을 정한 후 수행한다.

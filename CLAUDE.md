# Phi Brain — Claude Code 진입 안내

작업 전에 저장소 루트의 `AGENTS.md`를 직접 읽고 공통 작업 지침으로 적용한다.
이후 `docs/PRODUCT.md`, `docs/DESIGN.md`, `docs/STATUS.md`를 읽는다.

이 파일을 별도 기획서로 확장하지 않는다. Codex와 공유하는 요구사항·디자인·진행 상황은 `docs/`의 공통 문서에서 관리한다.
작업을 인계받을 때 최신 파일을 다시 읽고, 작업 종료 시 `docs/STATUS.md`를 갱신한다.
